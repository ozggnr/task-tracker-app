import { LoadingContainer, LoadingIcon, LoadingText } from './Loading.style';

export const Loading = () => {
    return (
        <LoadingContainer>
            <LoadingText>Loading</LoadingText>
            <LoadingIcon></LoadingIcon>
        </LoadingContainer>
    );
};
